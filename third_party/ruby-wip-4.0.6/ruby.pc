# pkg-config file for CosmoRuby
# Single source of truth for include paths and build flags

prefix=third_party/ruby
includedir=${prefix}/include
libdir=${prefix}

Name: CosmoRuby
Description: Ruby 4.0.6 ported to Cosmopolitan Libc
Version: 4.0.6
URL: https://www.ruby-lang.org/

# include paths used by Ruby compilation and tooling
Cflags: -I. \
        -I${includedir} \
        -I${prefix} \
        -I${prefix}/prism \
        -I${prefix}/enc/unicode/17.0.0 \
        -I${prefix}/ext/ripper \
        -I${prefix}/enc \
        -I${includedir}/ruby \
        -Ithird_party/zlib

# Library information (for tools that link against Ruby)
Libs: -L${libdir} -lruby